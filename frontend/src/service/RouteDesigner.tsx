import MapViewer from '@/component/map-viewer';

export function RouteDesigner() {
  return (
    /* CSS 적용 예정 (TaliWind CSS) */
    <div style={{ display: 'flex', width: '100vw', height: '100vh' }}>
      <MapViewer />
    </div>
  );
}
